<!DOCTYPE html>
<html lang="en">
<head>
  <!--
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="/js/jquery.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>

  <!-- Check if Session exists -->
  <nav>
    <% if session[:user_id].nil? %>
      <div id="login-signup">
        <button type="button" name="button"><a href="/session/new">Login</a></button>
        <button type="button" name="button"><a href="/user/new">Register</a></button>
      </div>
    <% else %>
      <div id="logout">
        <button type="button" name="button"><a href="/potluck/new">Host a Potluck</a></button>
        <form id="user-logout" action="/session/<%= session[:user_id] %>" method="post">
          <input type="hidden" name="_method" value="delete">
          <button type="submit" name="button">Logout</button>
        </form>
        <h1>Welcome Back <%= User.find(session[:user_id]).username %> </h1>
      </div>
    <% end %>
  </nav>

  <!-- Error Handler -->
  <div id="error-handler">
  <% if @error != nil %>
    <p><%= @error %></p>
  <% end %>
</div>

  <%= yield %>
</body>
</html>
